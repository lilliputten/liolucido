'use client';

import React from 'react';

import { SettingsCard } from './SettingsCard';

export function SettingsPageModalsWrapper() {
  /* // TODO: Receive modals' data from props...
   * const { showAddModal, deleteTopicId, editTopicId } = props;
   * // Process modals' and slots' handlers...
   * // Pass them to the wrapped component...
   */

  return <SettingsCard />;
}
