import React from 'react';

export default function EditTopicLayout({
  children,
  // selectLanguageModal,
}: {
  children: React.ReactNode;
  // selectLanguageModal: React.ReactNode;
}) {
  return (
    <>
      {children}
      {/*selectLanguageModal*/}
    </>
  );
}
